// axios.js
import axios from 'axios';

// const instance = axios.create({
//   baseURL: 'http://localhost:8080', // Replace with your API base URL
//   // Add any other configurations like headers, interceptors, etc.
// });

// // Add an interceptor to include the Authorization header for each request
// instance.interceptors.request.use(
//     (config) => {
//         const token = localStorage.getItem('sunbase_token'); // Replace with your actual Bearer Token or fetch it dynamically
//         if (token) {
//             config.headers.Authorization = `Bearer ${token}`;
//         }
//         return config;
//     },
//     (error) => {
//     return Promise.reject(error);
//     }
// );

// export default instance;
